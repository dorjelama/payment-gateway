import { Component } from '@angular/core';

@Component({
  selector: 'app-root', // Root component selector
  templateUrl: './app.component.html', // Template file
  styleUrls: ['./app.component.css'], // Optional styles
})
export class AppComponent {
  title = 'Payment App'; // Optional title
}